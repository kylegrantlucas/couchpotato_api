require 'spec_helper'

describe CouchpoataoApi do
  it 'has a version number' do
    expect(CouchpoataoApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
